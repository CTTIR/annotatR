% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-layer.R
\name{at_style}
\alias{at_style}
\title{Layer style}
\usage{
at_style(
  colour = NULL,
  fill_alpha = 0.3,
  stroke_width = 2,
  visible = TRUE,
  locked = FALSE,
  z = 1L,
  call = rlang::caller_env()
)
}
\arguments{
\item{colour}{Either \code{NULL} (default; the Okabe-Ito colourblind-safe palette
is cycled over the layer's labels), a single colour recycled across labels,
or a named character vector mapping labels to colours. The style is stored
with the resolved mapping so colours are stable across sessions.}

\item{fill_alpha}{Fill opacity in \verb{[0, 1]}. Default \code{0.3}.}

\item{stroke_width}{Stroke width in pixels. Default \code{2}.}

\item{visible}{Logical; whether the layer is drawn. Default \code{TRUE}.}

\item{locked}{Logical; whether the layer is protected from edits. Default
\code{FALSE}.}

\item{z}{Integer draw order (higher is drawn later, on top). Affects mask
overlap resolution. Default \code{1}.}

\item{call}{The calling environment, for error reporting.}
}
\value{
A named list of class \code{annot_style}.
}
\description{
Construct a style specification for an \link{annot_layer}.
}
\examples{
at_style(fill_alpha = 0.5, z = 2)
}
\seealso{
Other layers:
\code{\link[=at_layer]{at_layer()}},
\code{\link[=at_layer_add]{at_layer_add()}},
\code{\link[=at_layer_n]{at_layer_n()}},
\code{\link[=at_layer_remove]{at_layer_remove()}},
\code{\link[=at_layer_rois]{at_layer_rois()}}
}
\concept{layers}
