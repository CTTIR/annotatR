% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-roi.R
\name{at_roi_rescale}
\alias{at_roi_rescale}
\title{Rescale an ROI between pyramid levels}
\usage{
at_roi_rescale(roi, from_level, to_level, call = rlang::caller_env())
}
\arguments{
\item{roi}{An \link{annot_roi}.}

\item{from_level, to_level}{Integer source and target pyramid levels.}

\item{call}{The calling environment, for error reporting.}
}
\value{
An \link{annot_roi} with coordinates at \code{to_level} and its \code{level} field
set to \code{to_level}. The transform is exactly invertible.
}
\description{
Transform an ROI's coordinates from one pyramid level to another, assuming a
power-of-two pyramid. For an exact transform against a real image pyramid
(which need not be power-of-two), use \code{at_transform()} with the
\link{annot_image}.
}
\examples{
r <- at_roi_rect(0, 0, 100, 100, label = "a")
r2 <- at_roi_rescale(r, from_level = 0, to_level = 2)
identical(
  sf::st_coordinates(sf::st_geometry(r)),
  sf::st_coordinates(sf::st_geometry(at_roi_rescale(r2, 2, 0)))
)
}
\seealso{
\code{at_transform()}

Other geometry:
\code{\link[=at_check_geometry]{at_check_geometry()}},
\code{\link[=at_clamp]{at_clamp()}},
\code{\link[=at_fix_geometry]{at_fix_geometry()}},
\code{\link[=at_flip_y]{at_flip_y()}},
\code{\link[=at_roi_buffer]{at_roi_buffer()}},
\code{\link[=at_roi_contains]{at_roi_contains()}},
\code{\link[=at_roi_distance]{at_roi_distance()}},
\code{\link[=at_roi_overlaps]{at_roi_overlaps()}},
\code{\link{at_roi_setops}},
\code{\link[=at_roi_simplify]{at_roi_simplify()}},
\code{\link[=at_rois_overlap]{at_rois_overlap()}},
\code{\link[=at_snap]{at_snap()}},
\code{\link[=at_transform]{at_transform()}}
}
\concept{geometry}
