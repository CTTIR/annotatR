% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{at_check_containment}
\alias{at_check_containment}
\title{Check cross-layer containment}
\usage{
at_check_containment(project, tol = 0, call = rlang::caller_env())
}
\arguments{
\item{project}{An \link{annot_project}.}

\item{tol}{Non-negative slack, in level-0 pixels, by which the container is
grown before testing, to tolerate sub-pixel tracing overhang. Default \code{0}.}

\item{call}{The calling environment, for error reporting.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with columns \code{roi_id}, \code{layer}, \code{issue},
\code{severity} (\code{"warning"} for an ROI outside its container, \code{"error"} for a
missing/empty container), and \code{fixable} (always \code{FALSE}). A 0-row tibble
when every constrained ROI is contained, or when no layer declares a
\code{within} constraint.
}
\description{
Report ROIs in a constrained layer that fall outside their container region.
A layer declares the constraint through its metadata, e.g.
\code{at_layer("state", within = list(layer = "anatomy", label = "wound"))}; then
every ROI in \code{state} must be spatially covered by the union of the \code{anatomy}
layer's \code{wound} ROIs. This enforces the annotation rule that a class is
painted only inside its region of interest. The report mirrors the shape of
\code{\link[=at_check_geometry]{at_check_geometry()}}.
}
\examples{
anatomy <- at_layer_add(at_layer("anatomy", labels = "wound"),
                        at_roi_rect(2, 2, 8, 8, label = "wound"))
state <- at_layer("state", labels = "injury",
                  within = list(layer = "anatomy", label = "wound"))
state <- at_layer_add(state, at_roi_rect(0, 0, 4, 4, label = "injury"))
at_check_containment(at_project(at_example_image("cube"), list(anatomy, state)))
}
\seealso{
\code{\link[=at_check_geometry]{at_check_geometry()}}, \code{\link[=at_validate]{at_validate()}}

Other geometry:
\code{\link[=at_check_geometry]{at_check_geometry()}},
\code{\link[=at_clamp]{at_clamp()}},
\code{\link[=at_fix_geometry]{at_fix_geometry()}},
\code{\link[=at_flip_y]{at_flip_y()}},
\code{\link[=at_roi_buffer]{at_roi_buffer()}},
\code{\link[=at_roi_contains]{at_roi_contains()}},
\code{\link[=at_roi_distance]{at_roi_distance()}},
\code{\link[=at_roi_overlaps]{at_roi_overlaps()}},
\code{\link[=at_roi_rescale]{at_roi_rescale()}},
\code{\link[=at_roi_ring]{at_roi_ring()}},
\code{\link{at_roi_setops}},
\code{\link[=at_roi_simplify]{at_roi_simplify()}},
\code{\link[=at_rois_overlap]{at_rois_overlap()}},
\code{\link[=at_snap]{at_snap()}},
\code{\link[=at_transform]{at_transform()}}
}
\concept{geometry}
