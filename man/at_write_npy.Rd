% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-npy.R
\name{at_write_npy}
\alias{at_write_npy}
\title{Write a mask to a NumPy \code{.npy} file}
\usage{
at_write_npy(
  mask,
  path,
  dtype = c("int32", "uint8", "int16"),
  transpose = FALSE,
  legend = TRUE,
  overwrite = FALSE,
  call = rlang::caller_env()
)
}
\arguments{
\item{mask}{An \code{annot_mask}.}

\item{path}{Output file path.}

\item{dtype}{NumPy integer dtype: \code{"int32"} (default, wide enough for bitfield
codes), \code{"uint8"}, or \code{"int16"}.}

\item{transpose}{Logical; write the transpose, i.e. numpy shape
\verb{(width, height)} = \verb{[x, y]}, matching tools that store the native cube
order (e.g. TIVITA/HyperGui). Default \code{FALSE} writes the standard image
convention \verb{(height, width)}.}

\item{legend}{Logical; also write a \verb{<path>.legend.json} sidecar. Default
\code{TRUE}.}

\item{overwrite}{Logical; overwrite an existing file. Default \code{FALSE}.}

\item{call}{The calling environment, for error reporting.}
}
\value{
The output path, invisibly.
}
\description{
Write an \code{annot_mask} as a lossless integer NumPy array, for interchange with
Python tools. A \verb{<path>.legend.json} sidecar (value -> label) is written
alongside so the array stays self-describing. Unlike \code{\link[=at_write_mask]{at_write_mask()}}, values
are stored raw (no bit-depth normalisation), so bitfield artefact masks
survive intact.
}
\examples{
\dontshow{if (requireNamespace("magick", quietly = TRUE) || requireNamespace("tiff", quietly = TRUE)) withAutoprint(\{ # examplesIf}
m <- at_mask(at_example_project(), "labelled")
f <- tempfile(fileext = ".npy")
at_write_npy(m, f)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=at_read_npy]{at_read_npy()}}, \code{\link[=at_write_mask]{at_write_mask()}}

Other masks:
\code{\link[=at_mask]{at_mask()}},
\code{\link[=at_mask_agreement]{at_mask_agreement()}},
\code{\link[=at_mask_boundary]{at_mask_boundary()}},
\code{\link[=at_mask_derive]{at_mask_derive()}},
\code{\link[=at_mask_legend]{at_mask_legend()}},
\code{\link[=at_mask_preview]{at_mask_preview()}},
\code{\link[=at_mask_stack]{at_mask_stack()}},
\code{\link[=at_mask_stats]{at_mask_stats()}},
\code{\link[=at_read_mask]{at_read_mask()}},
\code{\link[=at_read_npy]{at_read_npy()}},
\code{\link[=at_write_mask]{at_write_mask()}},
\code{\link[=at_write_masks]{at_write_masks()}}
}
\concept{masks}
