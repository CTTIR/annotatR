% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-npy.R
\name{at_read_npy}
\alias{at_read_npy}
\title{Read a NumPy \code{.npy} integer mask}
\usage{
at_read_npy(
  path,
  level = 0L,
  transpose = FALSE,
  legend = NULL,
  call = rlang::caller_env()
)
}
\arguments{
\item{path}{Path to a \code{.npy} file.}

\item{level}{Integer pyramid level to record. Default \code{0}.}

\item{transpose}{Logical; transpose the array on read, for files stored in
the native cube order \verb{(width, height)} = \verb{[x, y]} (e.g. TIVITA/HyperGui
masks) so the result lands in annotatR's \verb{[y, x]} convention. Default
\code{FALSE} assumes the standard image convention \verb{(height, width)}.}

\item{legend}{Optional legend tibble (\code{value}, \code{label}) overriding the sidecar
/ pixel-value labelling.}

\item{call}{The calling environment, for error reporting.}
}
\value{
An \code{annot_mask}.
}
\description{
Read a NumPy integer array (e.g. a ground-truth annotation mask) into a
lossless \code{annot_mask} -- values are preserved exactly, including composite
bitfield codes. Use this (not \code{\link[=at_read_mask]{at_read_mask()}}, which polygonises) when the
raw integer matrix is what you need, for example to derive a training mask or
compute agreement against another mask.
}
\examples{
\dontshow{if (requireNamespace("magick", quietly = TRUE) || requireNamespace("tiff", quietly = TRUE)) withAutoprint(\{ # examplesIf}
m <- at_mask(at_example_project(), "labelled")
f <- tempfile(fileext = ".npy")
at_write_npy(m, f)
at_read_npy(f)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=at_write_npy]{at_write_npy()}}, \code{\link[=at_read_mask]{at_read_mask()}}

Other masks:
\code{\link[=at_mask]{at_mask()}},
\code{\link[=at_mask_agreement]{at_mask_agreement()}},
\code{\link[=at_mask_boundary]{at_mask_boundary()}},
\code{\link[=at_mask_derive]{at_mask_derive()}},
\code{\link[=at_mask_legend]{at_mask_legend()}},
\code{\link[=at_mask_preview]{at_mask_preview()}},
\code{\link[=at_mask_stack]{at_mask_stack()}},
\code{\link[=at_mask_stats]{at_mask_stats()}},
\code{\link[=at_read_mask]{at_read_mask()}},
\code{\link[=at_write_mask]{at_write_mask()}},
\code{\link[=at_write_masks]{at_write_masks()}},
\code{\link[=at_write_npy]{at_write_npy()}}
}
\concept{masks}
