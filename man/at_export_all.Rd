% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{at_export_all}
\alias{at_export_all}
\title{Export a whole session}
\usage{
at_export_all(
  session,
  dir,
  formats = c("mask_tiff", "geojson", "qupath", "rds", "csv"),
  scope = c("complete", "all", "current", "flagged"),
  mask_type = c("labelled", "binary", "multiclass"),
  level = 0L,
  per = "layer",
  overwrite = FALSE,
  progress = TRUE,
  call = rlang::caller_env()
)
}
\arguments{
\item{session}{An \link{annot_session}.}

\item{dir}{Output directory (created if needed).}

\item{formats}{Any of \code{"mask_tiff"}, \code{"geojson"}, \code{"qupath"}, \code{"rds"},
\code{"csv"}.}

\item{scope}{\code{"complete"} (default), \code{"all"}, \code{"current"}, or \code{"flagged"}.}

\item{mask_type}{Mask type for \code{"mask_tiff"}.}

\item{level}{Integer pyramid level. Default \code{0}.}

\item{per}{Mask split for \code{"mask_tiff"}; passed to \code{\link[=at_write_masks]{at_write_masks()}}.}

\item{overwrite}{Logical; overwrite existing files. Default \code{FALSE}.}

\item{progress}{Logical; show a progress bar. Default \code{TRUE}.}

\item{call}{The calling environment, for error reporting.}
}
\value{
An invisible export-receipt \link[tibble:tibble]{tibble::tibble} with columns \code{image},
\code{format}, \code{path}, \code{bytes}, \code{n_rois}, \code{status}, and \code{message}. Also written
to \verb{_export_manifest.csv} alongside the exports.
}
\description{
Export every selected image's annotations in the requested formats, isolating
per-image failures so one bad image never aborts the run.
}
\seealso{
\code{\link[=at_write_masks]{at_write_masks()}}, \code{\link[=at_manifest]{at_manifest()}}

Other batch:
\code{\link[=at_batch_apply]{at_batch_apply()}},
\code{\link[=at_batch_validate]{at_batch_validate()}},
\code{\link[=at_summary_table]{at_summary_table()}}
}
\concept{batch}
