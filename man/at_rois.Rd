% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-project.R
\name{at_rois}
\alias{at_rois}
\title{Query the ROIs of a project}
\usage{
at_rois(project, layer = NULL, label = NULL, call = rlang::caller_env())
}
\arguments{
\item{project}{An \link{annot_project}.}

\item{layer}{Optional single layer name to filter to.}

\item{label}{Optional character vector of labels to filter to.}

\item{call}{The calling environment, for error reporting.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per ROI and, in this exact order, the
columns:
\describe{
\item{\code{roi_id}}{Unique identifier (character).}
\item{\code{layer}}{Layer name (character).}
\item{\code{label}}{Class label (character).}
\item{\code{geom_type}}{Geometry type, e.g. \code{"POLYGON"} (character).}
\item{\code{level}}{Reference pyramid level (integer).}
\item{\code{area_px}}{Area in level-0 pixels (double); \code{NA} for points/lines.}
\item{\code{centroid_x},\code{centroid_y}}{Centroid at level 0 (double).}
\item{\code{n_vertices}}{Vertex count (integer).}
\item{\code{created},\code{modified}}{Timestamps (POSIXct).}
\item{\code{author}}{Author (character).}
\item{\code{source}}{Provenance source (character).}
\item{\code{geometry}}{The \code{sf} geometry column (\code{sfc}), at level 0.}
}
Returns a 0-row tibble with these columns if no ROIs match.
}
\description{
Return a tidy table of ROIs, optionally filtered by layer and/or label.
}
\details{
Coordinates in the \code{geometry} column, together with \code{area_px} and the
centroid, are expressed at pyramid level 0; the \code{level} column records the
level at which each ROI was originally defined.
}
\examples{
# proj <- at_example_project()
# at_rois(proj)
}
\seealso{
\code{\link[=at_layers]{at_layers()}}, \code{at_mask()}

Other projects:
\code{\link[=at_add_layer]{at_add_layer()}},
\code{\link[=at_add_roi]{at_add_roi()}},
\code{\link[=at_example_project]{at_example_project()}},
\code{\link[=at_layers]{at_layers()}},
\code{\link[=at_project]{at_project()}},
\code{\link[=at_remove_layer]{at_remove_layer()}},
\code{\link[=at_remove_roi]{at_remove_roi()}},
\code{\link[=at_summary]{at_summary()}},
\code{\link[=at_validate]{at_validate()}}
}
\concept{projects}
