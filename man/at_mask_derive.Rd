% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask.R
\name{at_mask_derive}
\alias{at_mask_derive}
\title{Derive a training mask from layer masks}
\usage{
at_mask_derive(
  state,
  anatomy,
  artefact = NULL,
  keep_label = "wound",
  background = 0L,
  call = rlang::caller_env()
)
}
\arguments{
\item{state}{An \code{annot_mask} of state (class) codes.}

\item{anatomy}{An \code{annot_mask} of anatomy codes, sharing \code{state}'s dimensions
and level.}

\item{artefact}{Optional \code{annot_mask} (bitfield); pixels with any artefact bit
set are dropped. \code{NULL} (default) applies no artefact exclusion.}

\item{keep_label}{The anatomy label whose region is kept (e.g. \code{"wound"}).}

\item{background}{Integer background/unlabeled value. Default \code{0}.}

\item{call}{The calling environment, for error reporting.}
}
\value{
An \code{annot_mask} (multiclass) of the surviving state codes, with the
state legend restricted to those classes and pixel counts recomputed.
}
\description{
Combine rasterised layer masks into the derived training mask
\verb{state WHERE anatomy == keep_label AND artefact == 0 AND state != background}.
This is the cross-layer operation the four-layer HSI scheme relies on: it
keeps a state (class) label only where the anatomy layer marks the region of
interest, no artefact bit is set, and the state is actually labelled.
}
\examples{
\dontshow{if (requireNamespace("magick", quietly = TRUE) || requireNamespace("tiff", quietly = TRUE)) withAutoprint(\{ # examplesIf}
proj <- at_example_project()
# A single-layer project derives trivially; the four-layer HSI workflow passes
# separate anatomy/state/artefact masks. See the masks vignette.
m <- at_mask(proj, "multiclass")
at_mask_derive(m, m, keep_label = at_mask_legend(m)$label[1])
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=at_mask]{at_mask()}}, \code{\link[=at_mask_stack]{at_mask_stack()}}

Other masks:
\code{\link[=at_mask]{at_mask()}},
\code{\link[=at_mask_agreement]{at_mask_agreement()}},
\code{\link[=at_mask_boundary]{at_mask_boundary()}},
\code{\link[=at_mask_legend]{at_mask_legend()}},
\code{\link[=at_mask_preview]{at_mask_preview()}},
\code{\link[=at_mask_stack]{at_mask_stack()}},
\code{\link[=at_mask_stats]{at_mask_stats()}},
\code{\link[=at_read_mask]{at_read_mask()}},
\code{\link[=at_read_npy]{at_read_npy()}},
\code{\link[=at_write_mask]{at_write_mask()}},
\code{\link[=at_write_masks]{at_write_masks()}},
\code{\link[=at_write_npy]{at_write_npy()}}
}
\concept{masks}
