% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agree.R
\name{at_mask_agreement}
\alias{at_mask_agreement}
\title{Agreement between two masks}
\usage{
at_mask_agreement(
  a,
  b,
  background = 0L,
  labels = NULL,
  call = rlang::caller_env()
)
}
\arguments{
\item{a}{Reference \code{annot_mask} or integer matrix.}

\item{b}{Comparison \code{annot_mask} or integer matrix, same dimensions as \code{a}.}

\item{background}{Integer value treated as unlabeled/background and excluded
from the per-class rows (still counted for overall accuracy/kappa).
Default \code{0}.}

\item{labels}{Optional named vector or legend tibble (\code{value}, \code{label})
overriding the reference legend for class labels.}

\item{call}{The calling environment, for error reporting.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per class: \code{value}, \code{label},
\code{n_true}, \code{n_pred}, \code{tp}, \code{fp}, \code{fn}, \code{dice}, \code{iou}. The \code{overall}
attribute is a list of \code{accuracy}, \code{kappa}, \code{mean_dice}, \code{mean_iou}, and
\code{n_px}.
}
\description{
Per-class Dice and Jaccard (IoU) plus an overall accuracy and Cohen's kappa,
comparing a reference mask \code{a} against a second mask \code{b} (e.g. an annotatR
mask against a \code{.npy} ground truth, or two annotators). Classes are matched by
integer value, so both masks must use the same class codebook.
}
\examples{
\dontshow{if (requireNamespace("magick", quietly = TRUE) || requireNamespace("tiff", quietly = TRUE)) withAutoprint(\{ # examplesIf}
m <- at_mask(at_example_project(), "multiclass")
ag <- at_mask_agreement(m, m)
attr(ag, "overall")$kappa
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=at_mask]{at_mask()}}, \code{\link[=at_read_npy]{at_read_npy()}}

Other masks:
\code{\link[=at_mask]{at_mask()}},
\code{\link[=at_mask_boundary]{at_mask_boundary()}},
\code{\link[=at_mask_derive]{at_mask_derive()}},
\code{\link[=at_mask_legend]{at_mask_legend()}},
\code{\link[=at_mask_preview]{at_mask_preview()}},
\code{\link[=at_mask_stack]{at_mask_stack()}},
\code{\link[=at_mask_stats]{at_mask_stats()}},
\code{\link[=at_read_mask]{at_read_mask()}},
\code{\link[=at_read_npy]{at_read_npy()}},
\code{\link[=at_write_mask]{at_write_mask()}},
\code{\link[=at_write_masks]{at_write_masks()}},
\code{\link[=at_write_npy]{at_write_npy()}}
}
\concept{masks}
