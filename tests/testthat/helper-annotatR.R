# Shared test fixtures for annotatR.
#
# Fixtures are deterministic: any randomness is seeded, and nothing depends on
# wall-clock time or locale. Helpers are populated alongside the code they
# exercise; see the per-phase test files for their use.

# Placeholder to keep the file non-empty during early scaffolding. Real
# fixtures (tiny_image(), square_roi(), demo_project(), ...) are added with the
# classes and mask engine they support.
NULL
